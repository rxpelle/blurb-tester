{
  "name": "grounds-reader",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.5.0",
    "@cloudflare/vitest-pool-workers": "^0.4.0",
    "wrangler": "^3.60.0"
  }
}
